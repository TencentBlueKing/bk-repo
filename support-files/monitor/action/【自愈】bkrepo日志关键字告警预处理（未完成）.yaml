name: 【自愈】bkrepo日志关键字告警预处理（未完成）
template_detail:
  ${alarm_callback_message}: '{{alarm.callback_message}}'
  ${alarm_current_value}: '{{alarm.current_value}}'
  ${alarm_data_source}: '{{alarm.data_source}}'
  ${alarm_detail_url}: '{{alarm.detail_url}}'
  ${alarm_dimension_string}: '{{alarm.dimension_string}}'
  ${alarm_name}: '{{alarm.name}}'
  ${alarm_related_info}: '{{alarm.related_info}}'
  ${alarm_target_type}: '{{alarm.target_type}}'
  ${devcloud_mnet_ip}: xxx.xxx.xxx.xxx
template_id: '14155'
timeout: 600
type: sops
version: '1.0'
