apiVersion: apps/v1
kind: Deployment
metadata:
  name: bkrepo-auth
  labels:
    tier: backend
    app: bkrepo-auth
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: bkrepo-auth
  template:
    metadata:
      labels:
        app: bkrepo-auth
    spec:
      containers:
        - name: bkrepo-auth
          image: {{ .Values.image.hub }}/bkrepo/auth:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          workingDir: /data/workspace/
          command: ['/bin/sh','-c','/data/workspace/startup.sh']
          env:
            {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            - name: HOST_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
          volumeMounts:
            - mountPath: /data/storage
              name: pv-storage
      volumes:
        - name: pv-storage
          persistentVolumeClaim:
            claimName: pvc-storage