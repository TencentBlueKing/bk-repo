{{- if .Values.docker.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: repo-docker
  labels:
    app: repo-docker
    {{- include "bkrepo.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.docker.replicaCount }}
  selector:
    matchLabels:
      app: repo-docker
  template:
    metadata:
      labels:
        app: repo-docker
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "bkrepo.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: repo-docker
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}/docker:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: BK_REPO_JVM_OPTION
              value: {{ .Values.common.jvmOption }}
            - name: BK_REPO_PROFILE
              value: {{ .Values.common.springProfile }}
          ports:
            - name: http
              containerPort: 25802
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /actuator/health/livenessState
              port: http
          readinessProbe:
            httpGet:
              path: /actuator/health/readinessState
              port: http
          startupProbe:
            httpGet:
              path: /actuator/health
              port: http
            failureThreshold: 20
            periodSeconds: 5
          volumeMounts:
            - name: repo-storage
              mountPath: {{ .Values.common.storage.filesystem.path }}
          resources:
            {{- toYaml .Values.docker.resources | nindent 12 }}
      volumes:
        - name: repo-storage
          {{- if not .Values.persistence.enabled }}
          emptyDir: {}
          {{- else if .Values.persistence.existingClaim }}
          persistentVolumeClaim:
            claimName: {{ .Values.persistence.existingClaim }}
          {{- else }}
          persistentVolumeClaim:
            claimName: repo-storage
          {{- end }}
      {{- with .Values.docker.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.docker.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}