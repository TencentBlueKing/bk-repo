{{- if .Values.fs-server.enabled -}}
apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
kind: Deployment
metadata:
  name: {{ include "common.names.fullname" . }}-fs-server
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: fs-server
    {{ include "bkrepo.labelValues.scope" . }}: {{ include "bkrepo.labelValues.scope.backend" . }}
    {{- if .Values.commonLabels }}
      {{- include "common.tplvalues.render" (dict "value" .Values.commonLabels "context" $) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" (dict "value" .Values.commonAnnotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  selector:
    matchLabels: {{- include "common.labels.matchLabels" . | nindent 6 }}
      app.kubernetes.io/component: fs-server
  replicas: {{ default 1 .Values.fs-server.replicaCount }}
  template:
    metadata:
      labels: {{- include "common.labels.standard" . | nindent 8 }}
        app.kubernetes.io/component: fs-server
        {{ include "bkrepo.labelValues.scope" . }}: {{ include "bkrepo.labelValues.scope.backend" . }}
        {{- if .Values.fs-server.podLabels }}
        {{- include "common.tplvalues.render" (dict "value" .Values.fs-server.podLabels "context" $) | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ template "bkrepo.serviceAccountName" . }}
      {{- include "bkrepo.imagePullSecrets" . | nindent 6 }}
      {{- if .Values.fs-server.hostAliases }}
      hostAliases: {{- include "common.tplvalues.render" (dict "value" .Values.fs-server.hostAliases "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.fs-server.affinity }}
      affinity: {{- include "common.tplvalues.render" ( dict "value" .Values.fs-server.affinity "context" $) | nindent 8 }}
      {{- else }}
      affinity:
        {{- if eq .Values.persistence.accessMode "ReadWriteOnce" }}
        podAffinity: {{- include "common.affinities.pods" (dict "type" "soft" "component" "repository" "context" $) | nindent 10 }}
        {{- else }}
        podAffinity: {{- include "common.affinities.pods" (dict "type" .Values.repository.podAffinityPreset "component" "fs-server" "context" $) | nindent 10 }}
        {{- end }}
        podAntiAffinity: {{- include "common.affinities.pods" (dict "type" .Values.fs-server.podAntiAffinityPreset "component" "fs-server" "context" $) | nindent 10 }}
        nodeAffinity: {{- include "common.affinities.nodes" (dict "type" .Values.fs-server.nodeAffinityPreset.type "key" .Values.fs-server.nodeAffinityPreset.key "values" .Values.fs-server.nodeAffinityPreset.values) | nindent 10 }}
      {{- end }}
      {{- if .Values.fs-server.nodeSelector }}
      nodeSelector: {{- include "common.tplvalues.render" ( dict "value" .Values.fs-server.nodeSelector "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.fs-server.tolerations }}
      tolerations: {{- include "common.tplvalues.render" (dict "value" .Values.fs-server.tolerations "context" .) | nindent 8 }}
      {{- end }}
      {{- if .Values.fs-server.priorityClassName }}
      priorityClassName: {{ .Values.fs-server.priorityClassName | quote }}
      {{- end }}
      {{- if .Values.fs-server.podSecurityContext.enabled }}
      securityContext: {{- omit .Values.fs-server.podSecurityContext "enabled" | toYaml | nindent 8 }}
      {{- end }}
      containers:
        {{- if .Values.bkstore.enabled }}
        - name: plugin-bkstore
          image: {{ include "bkrepo.images.image" ( dict "imageRoot" .Values.bkstore.image "global" .Values.global "bkrepo" .Values.common) }}
          args:
            - /bin/sh
            - -c
            - cp /data/workspace/plugin*jar /data/workspace/plugin/; sleep infinity
          volumeMounts:
            - name: shared-data
              mountPath: /data/workspace/plugin/
        {{- end }}
        {{- if .Values.gitci.enabled }}
        - name: plugin-gitci
          image: {{ include "bkrepo.images.image" ( dict "imageRoot" .Values.gitci.image "global" .Values.global "bkrepo" .Values.common) }}
          args:
            - /bin/sh
            - -c
            - cp /data/workspace/plugin*jar /data/workspace/plugin/; sleep infinity
          volumeMounts:
            - name: shared-data
              mountPath: /data/workspace/plugin/
        {{- end }}
        - name: fs-server
          image: {{ include "bkrepo.images.image" ( dict "imageRoot" .Values.fs-server.image "global" .Values.global "bkrepo" .Values.common) }}
          imagePullPolicy: {{ .Values.fs-server.image.pullPolicy }}
          {{- if .Values.fs-server.containerSecurityContext.enabled }}
          securityContext: {{- omit .Values.fs-server.containerSecurityContext "enabled" | toYaml | nindent 12 }}
          {{- end }}
          {{- if .Values.fs-server.resources }}
          resources: {{- toYaml .Values.fs-server.resources | nindent 12 }}
          {{- end }}
          env:
            - name: BK_REPO_JVM_OPTION
              value: {{ .Values.common.jvmOption }}
            - name: BK_REPO_PROFILE
              value: {{ .Values.common.springProfile }}
            - name: BK_REPO_SERVICE_PREFIX
              value: {{ include "common.names.fullname" . }}-
          ports:
            - name: http
              containerPort: 25801
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /actuator/health/livenessState
              port: http
            initialDelaySeconds: 120
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 5
            successThreshold: 1
          readinessProbe:
            httpGet:
              path: /actuator/health/readinessState
              port: http
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 5
            successThreshold: 1
          volumeMounts:
            - name: storage
              mountPath: {{ .Values.common.mountPath }}
            - name: shared-data
              mountPath: /data/workspace/plugin/
      volumes:
        - name: storage
          {{- if .Values.common.config.storage.nfs.enabled }}
          nfs:
            path: {{ .Values.common.config.storage.nfs.path }}
            server: {{ .Values.common.config.storage.nfs.server }}
          {{- else if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ if .Values.persistence.existingClaim }}{{ .Values.persistence.existingClaim }}{{- else }}{{ template "common.names.fullname" . }}-storage{{- end }}
          {{- else }}
          emptyDir: {}
          {{- end }}
        - name: shared-data
          emptyDir: {}
{{- end }}
