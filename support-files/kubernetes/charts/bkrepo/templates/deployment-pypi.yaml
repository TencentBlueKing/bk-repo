{{- if .Values.pypi.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: repo-pypi
  labels:
    app: repo-pypi
    {{- include "bkrepo.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.pypi.replicaCount }}
  selector:
    matchLabels:
      app: repo-pypi
  template:
    metadata:
      labels:
        app: repo-pypi
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "bkrepo.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: repo-pypi
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}/pypi:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: BK_REPO_JVM_OPTION
              value: {{ .Values.common.jvmOption }}
            - name: BK_REPO_PROFILE
              value: {{ .Values.common.springProfile }}
          ports:
            - name: http
              containerPort: 25805
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /actuator/health/livenessState
              port: http
          readinessProbe:
            httpGet:
              path: /actuator/health/readinessState
              port: http
          startupProbe:
            httpGet:
              path: /actuator/health
              port: http
            failureThreshold: 20
            periodSeconds: 5
          volumeMounts:
            - name: repo-storage
              mountPath: {{ .Values.common.storage.filesystem.path }}
          resources:
            {{- toYaml .Values.pypi.resources | nindent 12 }}
      volumes:
        - name: repo-storage
          {{- if not .Values.persistence.enabled }}
          emptyDir: {}
          {{- else if .Values.persistence.existingClaim }}
          persistentVolumeClaim:
            claimName: {{ .Values.persistence.existingClaim }}
          {{- else }}
          persistentVolumeClaim:
            claimName: repo-storage
          {{- end }}
      {{- with .Values.pypi.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.pypi.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}