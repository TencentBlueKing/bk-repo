{{- if .Values.common.config.storage.nfs.enabled -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bkrepo-nfs-pvc
spec:
  resources:
    requests:
      storage: 2Ti
  accessModes:
    - ReadWriteMany
  volumeName: bkrepo-nfs-pv
{{- end -}}