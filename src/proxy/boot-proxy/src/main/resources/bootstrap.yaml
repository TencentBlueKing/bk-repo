spring:
  application:
    name: proxy
  cloud:
    consul:
      enabled: false

server:
  port: 80

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.2
        database-dir: ./embedded-mongo
