spring:
  application:
    name: bkrepo-auth
  cloud:
    consul:
      discovery:
        heartbeat:
          enabled: true
        query-passing: true
        prefer-ip-address: true
      config:
        prefix: bkrepo-config
        format: YAML
        data-key: data
        profileSeparator: '::'

auth:
  realm: inner