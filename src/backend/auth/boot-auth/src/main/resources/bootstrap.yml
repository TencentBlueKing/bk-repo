server:
  port: 25902

spring.application.name: auth

auth:
  realm: local
  iam:
    callbackUser: bk_iam
  devops:
    authEnabled: false
    allowAnonymous: false


spring:
  cloud:
    config:
      uri: http://localhost:25906
  config:
    import:
