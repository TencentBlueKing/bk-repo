---
description: bk-repo media 模块开发指南，包含项目结构、代码风格、分层架构约定等，适用于后续在该模块中进行功能开发时参考。
globs:
alwaysApply: false
---

# bk-repo Media 模块开发指南

## 项目概述

bk-repo Media 模块是蓝鲸制品库的流媒体服务，提供视频推流/拉流、录制、转码等能力。基于 Kotlin + Spring Boot + MongoDB 构建。

## 模块结构

```
media/
├── common-media/       # 公共模块：数据模型(Model)、数据访问层(Dao)、公共 POJO
├── biz-media/          # 核心业务模块：Service 层、Controller 层、流处理逻辑
├── biz-media-job/      # 定时任务模块：转码任务调度等
├── biz-media-live/     # 直播相关模块
├── boot-media/         # 启动模块（主服务）
├── boot-media-job/     # 定时任务启动模块
└── boot-media-live/    # 直播启动模块
```

### 依赖关系

- `common-media` 仅依赖 `common-mongo`（提供 SimpleMongoDao 基类）
- `biz-media` 依赖 `common-media`、`artifact-service`、`api-schedule`
- `boot-*` 模块为各自的 Spring Boot 启动入口

## 分层架构约定

### 1. Model 层（common-media）

路径：`common-media/src/main/kotlin/com/tencent/bkrepo/media/common/model/`

- 使用 `@Document("collection_name")` 注解映射 MongoDB 集合
- 使用 `@CompoundIndex` 定义复合索引，推荐加 `background = true`
- 使用 `data class` 定义，字段使用 `var`
- `id` 字段类型为 `String?`，由 MongoDB 自动生成
- 时间字段使用 `java.time.LocalDateTime`

示例：

```kotlin
@Document("media_live_config")
@CompoundIndexes(
    CompoundIndex(
        name = "media_live_config_idx",
        def = "{'projectId': 1, 'userId': 1, 'workspaceId': 1}",
        unique = true,
        background = true,
    )
)
data class TMediaLiveConfig(
    var id: String?,
    var projectId: String,
    var userId: String,
    var workspaceId: String,
    var enabled: Boolean = true,
    var createdBy: String,
    var createdTime: LocalDateTime,
    var updatedBy: String,
    var updateTime: LocalDateTime,
)
```

### 2. Dao 层（common-media）

路径：`common-media/src/main/kotlin/com/tencent/bkrepo/media/common/dao/`

- 继承 `SimpleMongoDao<T>()`，泛型为对应 Model 类
- 使用 `@Repository` 注解
- 查询使用 Spring Data MongoDB 的 Kotlin DSL：`where(T::field).isEqualTo(value)`、`.and(T::field).isEqualTo(value)`
- 更新使用 `Update().set(T::field.name, value)`
- 常用方法：`findOne(query)`、`find(query)`、`updateFirst(query, update)`、`updateMulti(query, update)`、`remove(query)`、`insert(entity)`、`exists(query)`、`count(query)`
- 通过 `_id` 查询时使用：`org.springframework.data.mongodb.core.query.Criteria.where as strWhere`，然后 `strWhere("_id").isEqualTo(ObjectId(id))`

### 3. Service 层（biz-media）

路径：`biz-media/src/main/kotlin/com/tencent/bkrepo/media/service/`

- 使用 `@Service` 注解
- 通过构造函数注入 Dao 和其他 Service
- 需要 ArtifactService 能力的继承 `ArtifactService()`
- 日志使用 `LoggerFactory.getLogger(XxxService::class.java)`，放在 `companion object` 中
- 异常直接 throw，由框架统一处理

### 4. Controller 层（biz-media）

路径：`biz-media/src/main/kotlin/com/tencent/bkrepo/media/controller/`

#### 管理员接口（Admin）

- 路径前缀：`/api/admin/stream`
- 注解：`@Principal(PrincipalType.ADMIN)` 限制仅管理员访问
- 返回类型：`Response<T>`（`com.tencent.bkrepo.common.api.pojo.Response`）
- 使用 `ResponseBuilder.success(data)` 和 `ResponseBuilder.fail(code, msg)` 构造响应

#### 用户接口（User）

- 路径前缀：`/api/user/stream`
- 使用 `@Permission(ResourceType.REPO, PermissionAction.XXX)` 进行权限控制
- 使用 `PermissionManager.checkProjectPermission()` 进行项目级权限校验
- 使用 `@RequestAttribute userId` 获取当前登录用户

#### 内部接口

- 路径前缀：`/user/transcode`（无 `/api` 前缀）
- 通过临时 Token 鉴权

## 关键类和组件

| 类名 | 说明 |
|------|------|
| `StreamService` | 流管理核心服务：创建推流地址、发布流、RTC 拉流等 |
| `TranscodeService` | 视频转码服务：创建转码任务、处理回调 |
| `LiveConfigService` | 直播模式配置服务：管理新直播模式的启用/禁用 |
| `TokenService` | Token 管理服务：创建和验证临时访问 Token |
| `StreamManger` | 流管理器：管理活跃的流连接 |
| `MediaProperties` | 媒体服务配置属性 |
| `MediaArtifactInfo` | 媒体制品信息 |

## 常量

- `STREAM_PATH = "/streams"` — 流文件存储根路径（定义在 `Constants.kt`）

## 配置类（MediaProperties）

```kotlin
@ConfigurationProperties(prefix = "media")
class MediaProperties {
    var maxRecordFileSize: DataSize       // 最大录制文件大小
    var serverAddress: String             // 服务地址
    var grayServerAddress: String         // 灰度服务地址
    var transcodeConfig: Map<String, TranscodeConfig>  // 转码配置（按项目ID）
    var repoHost: String                  // 仓库主机地址
    var storageCredentialsKey: String?    // 存储凭证 Key
    var enabledLiveProjects: List<String> // 已启用直播的项目列表
}
```

## 新增功能开发流程

1. **Model**：在 `common-media/.../model/` 下创建 `TXxx.kt` 数据模型
2. **Dao**：在 `common-media/.../dao/` 下创建 `XxxDao.kt`，继承 `SimpleMongoDao<TXxx>()`
3. **Service**：在 `biz-media/.../service/` 下创建 `XxxService.kt`，注入 Dao
4. **Controller**：在 `biz-media/.../controller/` 下的对应 Controller 中添加接口
5. 如需 POJO/枚举，放在 `common-media/.../pojo/` 对应子目录下

## 编码风格

- 语言：Kotlin
- 构建工具：Gradle Kotlin DSL (`build.gradle.kts`)
- 包名格式：`com.tencent.bkrepo.media.{module}.{layer}`
- 使用 Spring Data MongoDB Kotlin 扩展进行查询构建
- 集合命名：`media_xxx_xxx`（小写下划线）
- 索引命名：`media_xxx_idx`