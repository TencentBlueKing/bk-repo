spring.application.name: npm
server.port: 25804

npm:
  migration:
    remote-registry:
    data-url:
  tarball:
    prefix:
  domain: ${cluster.self.url}/npm

spring:
  main:
    allow-circular-references: true
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
