#server:
#  port: 8888
#spring:
#  application:
#    name: config
#  cloud:
#    config:
#      server:
#        git:
##          uri: file:///data1/work/config/bkrepo-config
##          uri: file:///${user.home}/config
#          uri: https://github.com/spring-cloud-samples/config-repo
##          search-paths:
##            - config-server
##            - "config-server/{application}"
##        bootstrap: true
##        native:
##          searchLocations: file:///${user.home}/config/config-server
#  profiles:
#    consul:
#      discovery:
#        tags: dev
#    active: dev

#spring:
#  application:
#    name: config
#  cloud:
#    config:
#      server:
#        git:
#          uri: https://github.com/spring-cloud-samples/config-repo
##          basedir: target/config
#          default-label: main
#  profiles:
#    active: native
#
#server:
#  port: 8888

server:
  port: 8080
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: file:///data1/work/config/devops-config
          search-paths:
            - config-server
            - "config-server/{application}"
          default-label: develop
    consul:
      discovery:
        tags: dev

